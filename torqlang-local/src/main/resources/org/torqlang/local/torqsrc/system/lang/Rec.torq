package system.lang

meta#{'export', 'native': 'org.torqlang.klvm.RecMod'}
type Rec = {
    static {
        func assign[A, B](source::A, target::B) -> A & B,
        func size(rec::Rec) -> Int32,
    }
}
