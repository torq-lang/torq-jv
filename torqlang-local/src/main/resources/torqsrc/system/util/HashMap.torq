package system.util

// TODO

meta#{'export': true, 'native': 'org.torqlang.local.ArrayListPack'}
type HashMap = Obj & {
    // Required for Obj instances
    'new': func (params::Any...) -> Str,
    'equals': func (other::Any) -> Bool,
    'hash_code': func () -> Int32,
}

meta#{'export': true, 'native': 'org.torqlang.local.ArrayListPack'}
type HashMaps = Obj & {
    // Required for Obj companions
    'equals': func (other::Any) -> Bool,
    'hash_code': func () -> Int32,
}
